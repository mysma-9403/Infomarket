<div class="col-xs-12">
	<div class="row equal">
		<div class="col-xs-6 col-sm-2">
			<h2>{{ 'label.category.children'|trans }}</h2>
		</div>
		{% if is_granted('ROLE_EDITOR') %}
			<div class="col-xs-6 col-sm-2">
				<a href="{{ path('admin_menu_entry_new', { parent: entry.id }) }}"
					class="btn btn-success text-xs-bottom-mm">
					<span class="fa fa-plus"></span> 
					{{ 'action.menuEntry.createSubentry'|trans }} 
				</a>
			</div>
		{% endif %}
	</div>
</div>

<table class="table table-striped vert-align">
	<tbody>
	{% for subentry in entry.children %}
		<tr>
			<td>
				<a href="{{ path('admin_menu_entry_show', { id: subentry.id }) }}">
					{{ subentry.name }}
				</a>
			</td>
			<td>
				{% include 'common/fields/bool_action_field.html.twig' with 
					{ 'id': subentry.id, 'value': subentry.infomarket, 'extPath': '_set_im_published',
						'trueTooltip': 'label.published'|trans, 'falseTooltip': 'label.unpublished'|trans } %}
			</td>
			<td>
				{% include 'common/fields/bool_action_field.html.twig' with 
					{ 'id': subentry.id, 'value': subentry.infoprodukt, 'extPath': '_set_ip_published',
						'trueTooltip': 'label.published'|trans, 'falseTooltip': 'label.unpublished'|trans } %}
			</td>
			{% if is_granted('ROLE_EDITOR') %}
				<td>
					<a href="{{ path('admin_menu_entry_edit', { id: subentry.id }) }}" class="btn btn-warning">
						<span class="fa fa-edit"></span> 
						{{ 'action.edit'|trans }}
					</a>
					{% if is_granted('ROLE_ADMIN') %}
						<a href="{{ path('admin_menu_entry_delete', { id: subentry.id }) }}" class="btn btn-danger">
							<span class="fa fa-trash"></span> 
							{{ 'action.delete'|trans }}
						</a>
					{% endif %}
				</td>
			{% endif %}
		</tr>
	{% endfor %}
	</tbody>
</table>



<div class="col-xs-12">
	<div class="row equal">
		<div class="col-xs-6 col-sm-2">
			<h2>{{ 'title.menus'|trans }}</h2>
		</div>
		{% if is_granted('ROLE_EDITOR') %}
			<div class="col-xs-6 col-sm-2">
				<a href="{{ path('admin_menu_menu_entry_assignment_new', { menu_entry: entry.id }) }}"
					class="btn btn-success text-xs-bottom-mm">
					<span class="fa fa-plus"></span> 
					{{ 'action.assignMenu'|trans }} 
				</a>
			</div>
		{% endif %}
	</div>
</div>

<table class="table table-striped vert-align">
	<tbody>
	{% for menuMenuEntryAssignment in entry.menuMenuEntryAssignments %}
		<tr>
			<td>
				<a href="{{ path('admin_menu_show', { id: menuMenuEntryAssignment.menu.id }) }}">
					{{ menuMenuEntryAssignment.menu.name }}
				</a>
			</td>
			{% if is_granted('ROLE_EDITOR') %}
				<td>
					<a href="{{ path('admin_menu_menu_entry_assignment_edit', { id: menuMenuEntryAssignment.id }) }}" class="btn btn-warning">
						<span class="fa fa-edit"></span> 
						{{ 'action.edit'|trans }}
					</a>
					<a href="{{ path('admin_menu_menu_entry_assignment_delete', { id: menuMenuEntryAssignment.id }) }}" class="btn btn-danger">
						<span class="fa fa-trash"></span> 
						{{ 'action.delete'|trans }}
					</a>
				</td>
			{% endif %}
		</tr>
	{% endfor %}
	</tbody>
</table>



<div class="col-xs-12">
	<div class="row equal">
		<div class="col-xs-6 col-sm-2">
			<h2>{{ 'title.branches'|trans }}</h2>
		</div>
		{% if is_granted('ROLE_EDITOR') %}
			<div class="col-xs-6 col-sm-2">
				<a href="{{ path('admin_menu_entry_branch_assignment_new', { menu_entry: entry.id }) }}"
					class="btn btn-success text-xs-bottom-mm">
					<span class="fa fa-plus"></span> 
					{{ 'action.assignBranch'|trans }} 
				</a>
			</div>
		{% endif %}
	</div>
</div>

<table class="table table-striped vert-align">
	<tbody>
	{% for menuEntryBranchAssignment in entry.menuEntryBranchAssignments %}
		<tr>
			<td>
				<div class="table-circle-30 bkg-white">
					{% if menuEntryBranchAssignment.branch.image %}
						<img class="div-center img-{{ menuEntryBranchAssignment.branch.vertical ? '' : 'v' }}responsive"
							alt="{{ menuEntryBranchAssignment.branch.name }}" src="{{ asset(menuEntryBranchAssignment.branch.getImagePath()) }}" />
					{% endif %}
				</div>
			</td>
			<td>
				<a href="{{ path('admin_branch_show', { id: menuEntryBranchAssignment.branch.id }) }}">
					{{ menuEntryBranchAssignment.branch.name }}
				</a>
			</td>
			{% if is_granted('ROLE_EDITOR') %}
				<td>
					<a href="{{ path('admin_menu_entry_branch_assignment_edit', { id: menuEntryBranchAssignment.id }) }}" class="btn btn-warning">
						<span class="fa fa-edit"></span> 
						{{ 'action.edit'|trans }}
					</a>
					<a href="{{ path('admin_menu_entry_branch_assignment_delete', { id: menuEntryBranchAssignment.id }) }}" class="btn btn-danger">
						<span class="fa fa-trash"></span> 
						{{ 'action.delete'|trans }}
					</a>
				</td>
			{% endif %}
		</tr>
	{% endfor %}
	</tbody>
</table>

<div class="col-xs-12">
	<div class="row equal">
		<div class="col-xs-6 col-sm-2">
			<h2>{{ 'title.categories'|trans }}</h2>
		</div>
		{% if is_granted('ROLE_EDITOR') %}
			<div class="col-xs-6 col-sm-2">
				<a href="{{ path('admin_menu_entry_category_assignment_new', { menu_entry: entry.id }) }}"
					class="btn btn-success text-xs-bottom-mm">
					<span class="fa fa-plus"></span> 
					{{ 'action.assignCategory'|trans }} 
				</a>
			</div>
		{% endif %}
	</div>
</div>

<table class="table table-striped vert-align">
	<tbody>
	{% for menuEntryCategoryAssignment in entry.menuEntryCategoryAssignments %}
		<tr>
			<td>
				<div class="table-circle-30 bkg-white">
					{% if menuEntryCategoryAssignment.category.image %}
						<img class="div-center img-{{ menuEntryCategoryAssignment.category.vertical ? '' : 'v' }}responsive"
							alt="{{ menuEntryCategoryAssignment.category.name }}" src="{{ asset(menuEntryCategoryAssignment.category.getImagePath()) }}" />
					{% endif %}
				</div>
			</td>
			<td>
				<a href="{{ path('admin_category_show', { id: menuEntryCategoryAssignment.category.id }) }}">
					{{ menuEntryCategoryAssignment.category.name }}
				</a>
			</td>
			{% if is_granted('ROLE_EDITOR') %}
				<td>
					<a href="{{ path('admin_menu_entry_category_assignment_edit', { id: menuEntryCategoryAssignment.id }) }}" class="btn btn-warning">
						<span class="fa fa-edit"></span> 
						{{ 'action.edit'|trans }}
					</a>
					<a href="{{ path('admin_menu_entry_category_assignment_delete', { id: menuEntryCategoryAssignment.id }) }}" class="btn btn-danger">
						<span class="fa fa-trash"></span> 
						{{ 'action.delete'|trans }}
					</a>
				</td>
			{% endif %}
		</tr>
	{% endfor %}
	</tbody>
</table>