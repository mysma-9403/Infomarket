{% extends 'common/menu/compound_link.html.twig' %}

{% set badge = unreadMessagesCount > 0 ? unreadMessagesCount : '' %}

{% set sublinks = [
	{type: 'link', path: 'benchmark_field', label: 'label.benchmarkFields'},
	{type: 'link', path: 'benchmark_enum', label: 'label.benchmarkEnums'},
	{type: 'link', path: 'benchmark_message', label: 'label.benchmarkMessages', 
		badge: (unreadMessagesCount > 0 ? unreadMessagesCount : '') }
] %}