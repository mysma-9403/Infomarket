{% extends 'infomarket/base/base.html.twig' %}

{% set title = title|default(entry.name) %}

{% set extPath = extPath|default('_show') %}

{% block moreBreadcrumbs %}

	/ 
	<a href="{{ path(basePath ~ mainPath) }}">
		{{ type|trans }}
	</a>
	{% block baseMoreBreadcrumbs%}
	{% endblock %}
	/ 
	<a href="{{ path(basePath ~ mainPath ~ '_show', { id: entry.id }) }}">
		{{ title|trans }}
	</a>
	
{% endblock %}