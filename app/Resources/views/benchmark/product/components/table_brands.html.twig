<tr>
	<th class="text-right">{{ 'label.brand'|trans }}</th>
	
	{% for entry in entries|slice(0,half) %}
		<th class="text-center">{{ entry.brandName }}</th>
	{% endfor %}
	<th class="selected text-center">{{ entry.brand.name }}</th>
	{% for entry in entries|slice(half) %}
		<th class="text-center">{{ entry.brandName }}</th>
	{% endfor %}
	
	<th></th>
</tr>